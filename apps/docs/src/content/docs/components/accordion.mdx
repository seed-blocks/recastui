---
title: 'Accordion - Recastui'
description: 'An accordion is a vertically stacked set of interactive headings containing a title, content snippet, or thumbnail representing a section of content.'
---
import { Accordion } from '@recastui/react';
import { Playground } from '@components/Playground';
import { CodeBlock } from '@components/CodeBlock';
import { accordionSnippets } from '@components/configs/accordion';

# Accordion

An accordion is a vertically stacked set of interactive headings containing a title, content snippet, or thumbnail representing a section of content.

## Import
<CodeBlock code={ accordionSnippets.import } client:visible/>

## Usage
<Playground code={ accordionSnippets.usage } client:visible/>

### Variants
Use the *variant* prop to change the visual style of the Accordion. You can set the value to *'boxed', 'filled' or 'spaced'*. It's an optional prop.
<Playground code={ accordionSnippets.variants } client:visible/>

### Left Icon
Use the *iconLeft* prop to change the position of the Accordion Chevron/Icon from right to left.
<Playground code={ accordionSnippets.iconLeft } client:visible/>

### Disable Icon Rotation
Use the *disableIconRotation* prop to disable the rotation of the Accordion Chevron/Icon.
<Playground code={ accordionSnippets.disableIconRotation } client:visible/>

### Transition Duration
Use the *transitionDuration* prop to change the duration of Accordion Panel height transition.
<Playground code={ accordionSnippets.transitionDuration } client:visible/>

### Icon Rotation Degree
Use the *iconRotationClassName* prop to change the rotation degree of the Icon.
<Playground code={ accordionSnippets.iconRotationClassName } client:visible/>

## Recipes

### Custom Icon
Use the *icon* prop to change the Icon.
<Playground code={ accordionSnippets.icon } client:visible/>


