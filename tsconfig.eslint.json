{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		// ensure that nobody can accidentally use this config for a build
		"noEmit": true
	},
	"include": ["packages/**/src", "apps/**/src", "jest.setup.ts"],
	"exclude": ["node_modules", "**/dist", "**/build", "**/esm", "**/cjs", "**/ts"]
}
